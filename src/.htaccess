# Do not remove this line or mod_rewrite rules and search engine friendly URLs will stop working
RewriteBase /

RewriteEngine on
RewriteCond %{HTTP_HOST} ^beta\.cword\.cf
RewriteRule ^(.*)$ https://cword.cf/beta/$1 [R=301]